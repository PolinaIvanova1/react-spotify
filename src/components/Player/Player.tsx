import React from 'react';
import Box from "@mui/material/Box";


const Player = ({token, trackUri}: any) => {

    if (!token) return null
    return (
       <Box></Box>
    );
};

export default Player;